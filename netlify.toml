[build]
  command = "bun install --frozen-lockfile && bun run --cwd showcase build"
  publish = "showcase/dist"

[build.environment]
  BUN_VERSION  = "1.3.12"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
